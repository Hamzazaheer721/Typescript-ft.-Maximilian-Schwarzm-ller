const array: string[] = ["Apples", "Mangoes", "Pears"]

const elem: string = Math.floor(Math.random() * array.length);

console.info(array[elem]) // Pears
console.info(array[elem]) // Apples
console.info(array[elem]) // Pears
console.info(array[elem]) // Mangoes
